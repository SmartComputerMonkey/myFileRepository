1、变量名命名规则
    1）变量名的用途定义变量名，并且在变量名的末尾处加 _变量类型，例如：Integer num_Integer = new Integer();
2、函数命名规则
    1）按方法用途命名方法名，并且在方法名的末尾处加 _返回值类型  例如：Integer getNumber_Integer(){};
3、类名的命名规则
    1）根据业务定义类名，
    2）如果是接口，则在类名末尾处加 _Interface
    3）如果是某接口的实现类，则在类名末尾处添加 _InterfaceImpl


添加注释：
    添加日期，姓名， 修改内容等信息